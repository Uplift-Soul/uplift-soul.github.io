# Backend / Pi-only — must NOT be served publicly by GitHub Pages
publish.py
.env
.env.*

# Python noise (in case the script is run/edited locally)
__pycache__/
*.pyc
.venv/
venv/

# Brand design-system package + concept mockups — a design deliverable synced to
# Claude Design, not part of the public GitHub Pages site.
design-system/
brand-concepts/
